{
  "importMap": "./import_map.json",
  "tasks": {
    "test": "deno test --allow-env --allow-read --allow-run test",
    "test-esbuild-compiler": "deno run -q --import-map import_map.json -A lib/proscenium/compilers/esbuild.js",
    "compile-esbuild": "deno compile --no-config -o bin/esbuild --import-map import_map.json -A lib/proscenium/compilers/esbuild.js"
  }
}
